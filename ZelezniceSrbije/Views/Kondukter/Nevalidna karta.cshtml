@{
    ViewData["Title"] = "Nevalidna karta";
}

<style>
    :root {
        --sz-red: #CC0000;
        --sz-blue: #003B8E;
    }

    .result-wrap {
        max-width: 560px;
        margin: 2.5rem auto;
    }

    .result-card {
        border: 1px solid #dbe4f0;
        border-radius: 14px;
        overflow: hidden;
    }

    .result-body {
        padding: 2rem 1.35rem 1.4rem;
        text-align: center;
    }

    .result-state {
        display: inline-block;
        background: #fff1f1;
        color: #b71c1c;
        border: 1px solid #f2c0c0;
        border-radius: 999px;
        padding: .35rem .8rem;
        font-size: .82rem;
        font-weight: 600;
        margin-bottom: .9rem;
    }

    .result-title {
        color: #b71c1c;
        margin-bottom: .65rem;
    }

    .btn-sz-outline {
        background: #fff;
        border: 1px solid var(--sz-blue);
        color: var(--sz-blue);
        font-weight: 600;
    }

    .btn-sz-outline:hover {
        background: var(--sz-blue);
        border-color: var(--sz-blue);
        color: #fff;
    }
</style>

<div class="result-wrap">
    <div class="card result-card shadow-sm">
        <div class="result-body">
            <span class="result-state">Neuspešno</span>
            <h2 class="result-title h3">Nevalidna karta</h2>
            <p class="text-muted fs-5">Ova karta nije validna za trenutnu vožnju ili je već poništena.</p>
            <div class="mt-4">
                <a asp-controller="Kondukter" asp-action="Panel" class="btn btn-sz-outline btn-lg w-100">Nazad na panel</a>
            </div>
        </div>
    </div>
</div>
